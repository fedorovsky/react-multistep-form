import * as React from 'react';
import * as Styled from './PageLogin.styled';

const PageLogin = () => {
  return (
    <Styled.Wrapper>
      <h1>Page Login</h1>
    </Styled.Wrapper>
  );
};

export default PageLogin;
