import * as React from 'react';
import * as Styled from './Login.styled';

const Login = () => {
  return (
    <Styled.Wrapper>
      <h1>Login</h1>
    </Styled.Wrapper>
  );
};

export default Login;
